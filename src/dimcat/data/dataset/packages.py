import logging

from dimcat.data.dataset.base import DimcatPackage

logger = logging.getLogger(__name__)


class ScorePackage(DimcatPackage):
    """A package containing resources that are (references to) scores."""

    pass
